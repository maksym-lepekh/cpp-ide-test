{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/cmake-build-default/ide-test",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}